spring.application.name=SpringEcom
spring.datasource.url=jdbc:postgresql://localhost:5432/ecom
spring.datasource.username=postgres
spring.jpa.hibernate.ddl-auto=update

# Allow larger image uploads (default is 1MB per file)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# OAuth2 Google Configuration
spring.security.oauth2.client.registration.google.scope=openid,profile,email
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}
jwt.secretKey=asdfhads9f67as98dfvaisudhfa98s67dfy89aishudfuavs89dfvasi8df76a89sdf7y
spring.datasource.password=Haris@285046
spring.security.oauth2.client.registration.google.client-id=665583127285-mjga2445hur83p0rb4cbj6mks4etrd3c.apps.googleusercontent.com
spring.security.oauth2.client.registration.google.client-secret=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

# ─── Redis Cache Configuration ───────────────────────────────────────────────
spring.data.redis.host=localhost
spring.data.redis.port=6379
# No password set on local Redis instance
# Force Spring to use Redis (not in-memory fallback)
spring.cache.type=redis
# Default cache TTL: 10 minutes (so stale data auto-expires even if eviction misses)
spring.cache.redis.time-to-live=600000
